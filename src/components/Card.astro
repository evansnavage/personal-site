---
const { title, description, image } = Astro.props;
---

<div class="card">
  <img src={image} alt={title} class="image-cover" />
  <div class="p-4">
    <h3 class="text-xl font-bold mb-2">{title}</h3>
    <p class="text-content">{description}</p>
  </div>
</div>
